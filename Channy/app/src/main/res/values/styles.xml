<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="android:Theme.DeviceDefault.Light">
        <!-- Customize your theme here. -->
        <item name="android:colorPrimary">@color/colorPrimary</item>
        <item name="android:colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="android:colorAccent">@color/colorAccent</item>
        <item name="android:actionMenuTextColor">@color/actionMenuTextColor</item>
        <!--<item name="android:navigationIcon">@drawable/absolutechild</item>-->
        <item name="android:titleTextColor">@color/colorControlNormal</item>
    </style>

    <style name="AppBaseTheme" parent="android:Theme.DeviceDefault">
        <item name="android:actionMenuTextColor">@color/toolbar_color</item>
    </style>

    <style name="AppDarkBar" parent="android:Theme.DeviceDefault.Light.DarkActionBar">
        <item name="android:colorControlNormal">@color/colorControlNormal</item>
    </style>

</resources>
